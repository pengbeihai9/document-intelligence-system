# 十篇文献本地摘要测试结果

测试模式：强制关闭大模型 API，只测试本地规则兜底摘要。

测试数量：10

## 1. A survey on vision transformer

- 输入长度：1338 字符
- 原文长度记录：152620
- 语言：en
- 摘要来源：本地摘要
- 摘要长度：285 字符
- 低质量检测：否

该研究围绕transformer、vision、based、attention、computer、processing、neural、network等主题展开，核心内容为：—Transformer, ﬁrst applied to the ﬁeld of natural language processing, is a type of deep neural network mainly based on the self-attention mechanism.；Thanks to its strong representation capabilities。

## 2. A ViT：Adaptive Tokens for Efficient Vision Transformer

- 输入长度：3887 字符
- 原文长度记录：45279
- 语言：en
- 摘要来源：本地摘要
- 摘要长度：543 字符
- 低质量检测：否

该研究围绕vision、transformers、image、vit、network、have、inference、tokens等主题展开，核心内容为：We introduce A-ViT, a method that adaptively adjusts the inference cost of vision transformer (ViT) for images of different complexity.；A-ViT achieves this by automatically reducing the number of tokens in vision transformers that are processed in the。结论上，We have introduced A-ViT to adaptively adjust the amount of token computation based on input complexity.；We demonstrated that the method improves vision transformer throughput on hardware without imposing extra。

## 3. An efficient spatio temporal pyramid transformer for action detection

- 输入长度：1499 字符
- 原文长度记录：57661
- 语言：en
- 摘要来源：本地摘要
- 摘要长度：323 字符
- 低质量检测：否

该研究围绕action、detection、end、long、video、efficient、stpt、attention等主题展开，核心内容为：The task of action detection aims at deducing both the action category and localization of the start and end moment for each action instance in a long, untrimmed video.；While vision Transformers have driven the recent advances in video understanding。

## 4. Anchor detr Query design for transformer based detector

- 输入长度：2760 字符
- 原文长度记录：37339
- 语言：en
- 摘要来源：本地摘要
- 摘要长度：478 字符
- 低质量检测：否

该研究围绕query、anchor、can、attention、design、object、detector、based等主题展开，核心内容为：In this paper, we propose a novel query design for the transformer-based object detection.；In previous transformerbased detectors, the object queries are a set of learned embeddings。结论上，In this paper, we propose a detector based on the transformer.；We propose a novel query design based on the anchor point that has explicit physical meaning.；The corresponding prediction slots can have a speciﬁc mode, i.。

## 5. BEHRT transformer for electronic health records

- 输入长度：9094 字符
- 原文长度记录：57516
- 语言：en
- 摘要来源：本地摘要
- 摘要长度：937 字符
- 低质量检测：是

核心概述：该文档初步判断为其他文档，主要围绕模型等内容展开。Each individual’s EHR can link data from many sources (e.；Each instance of a concept can mean a single or multiple data points.。
关键信息：This makes large-scale EHR a uniquely rich source of insight and an unrivalled data for training data-hungry ML models.；” or, “which features should have interactions with one another。
重要结论：One of the earliest works in applying deep learning to EHR, Liang et al.；These early works on the application of DL to EHR did not take into account the subtleties of EHR data (e.。
风险或注意事项：1 introduced a DL model that can predict the risk of lung cancer from a patient’s tomography images with 94.；2 showed that DL can predict a range of cardiovascular risk factors from just a retinal。
建议行动：The latest developments in machine learning (including deep learning) provides a great opportunity to address this unmet need.；” or, “which features should have interactions with one another。

## 6. Cat：Cross attention in vision transformer

- 输入长度：6159 字符
- 原文长度记录：48898
- 语言：en
- 摘要来源：本地摘要
- 摘要长度：546 字符
- 低质量检测：否

该研究围绕attention、transformer、cnn、feature、tasks、computation、which、network等主题展开，核心内容为：Since Transformer has found widespread use in NLP, the potential of Transformer in CV has been realized and has inspired many new approaches.；However, the computation required for replacing word tokens with image patches for Transformer after the。结论上，In this paper, the proposed Cross Attention is proposed to better combine the virtue of local feature extraction in CNN with the virtue of global information extraction in Transformer, and build a robust backbone。

## 7. Chemformer：a pre trained transformer for computational chemistry

- 输入长度：2835 字符
- 原文长度记录：54935
- 语言：en
- 摘要来源：本地摘要
- 摘要长度：347 字符
- 低质量检测：否

该研究围绕模型、结果验证等主题展开，核心内容为：Transformer models coupled with a simplified molecular line entry system (SMILES) have recently proven to be a powerful combination for solving challenges in cheminformatics。结论上，In this work we introduced the Chemformer model, which makes use of the SMILES language for application to diverse computational chemistry tasks。

## 8. Contextual transformer networks for visual recognition

- 输入长度：7839 字符
- 原文长度记录：55300
- 语言：en
- 摘要来源：本地摘要
- 摘要长度：515 字符
- 低质量检测：否

该研究围绕attention、transformer、keys、contextual、block、context、self、cot等主题展开，核心内容为：Transformer with self-attention has led to the revolutionizing of natural language processing ﬁeld, and recently inspires the emergence of Transformer-style architecture design with competitive results in numerous computer vision tasks。结论上，In this work, we propose a new Transformer-style architecture, termed Contextual Transformer (CoT) block, which exploits the contextual information among input keys to guide self-attention learning。

## 9. Crossvit：Cross attention multi scale vision transformer for image classification

- 输入长度：2512 字符
- 原文长度记录：51529
- 语言：en
- 摘要来源：本地摘要
- 摘要长度：429 字符
- 低质量检测：否

该研究围绕transformer、image、vision、classification、multi、scale、models、tokens等主题展开，核心内容为：The recently developed vision transformer (ViT) has achieved promising results on image classification compared to convolutional neural networks.；Inspired by this, in this paper。结论上，In this paper, we present CrossViT, a dual-branch vision transformer for learning multi-scale features, to improve the recognition accuracy for image classification。

## 10. Cswin transformer：A general vision transformer backbone with cross shaped windows

- 输入长度：3305 字符
- 原文长度记录：51826
- 语言：en
- 摘要来源：本地摘要
- 摘要长度：493 字符
- 低质量检测：否

该研究围绕transformer、attention、cswin、tasks、self、vision、under、stripe等主题展开，核心内容为：We present CSWin Transformer, an efﬁcient and effective Transformer-based backbone for general-purpose vision tasks.；A challenging issue in Transformer design is that global self-attention is very expensive to compute whereas local self-attention often。结论上，In this paper, we have presented a new Vision Transformer architecture named CSWin Transformer.；The core design of CSWin Transformer is the CSWin Self-Attention。

## 汇总

- 共测试 10 篇文献。
- 低质量检测标记 1 篇。
- 本测试用于验证大模型掉线时的本地兜底稳定性，不代表在线大模型摘要上限。
